package Formation;

import org.junit.Test;

import static org.junit.Assert.*;

public class LocationTest {

    @Test(timeout = 15)
    public void randomHashSet() {
        new Location().randomHashSet();
    }
}